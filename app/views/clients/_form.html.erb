<script type="text/javascript">
  $(function() {
    $('.date_picker').datetimepicker({
    });
  });
</script>

<%= simple_form_for(@client) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.association :division %>
    <%= f.input :region %>
    <%= f.input :launch_date, :as => :date_picker %>
    <%= f.input :internal %>
    <%= f.association :price %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
